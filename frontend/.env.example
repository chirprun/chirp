# Where `npm run dev` / `npm run preview` should proxy /api and /mcp (must match your uvicorn port).
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Production build: full API origin (no proxy). Leave unset in dev if you use the proxy above.
# VITE_API_URL=https://your-api.example.com
